import { Router } from "express";

const router = Router();

router.post("/login", () => {
  /* todo llamar al controlador */
});

router.post("/register", () => {
  /* Todo llamar al controlador */
});

router.post("/logout", () => {
  /*Todo llamar al controlador*/
});

export default router;
